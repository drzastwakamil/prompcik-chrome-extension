{
  "name": "fake-news-filter",
  "version": "1.0.0",
  "description": "Fact-check content on any webpage to identify potentially misleading information",
  "type": "module",
  "scripts": {
    "dev": "npm run build && npm run watch",
    "watch": "npm run watch:content & npm run watch:background",
    "watch:content": "vite build --config vite.config.js --watch --mode development",
    "watch:background": "vite build --config vite.config.background.js --watch --mode development",
    "build": "npm run clean && npm run build:content && npm run build:background && node scripts/post-build.js",
    "build:content": "vite build --config vite.config.js",
    "build:background": "vite build --config vite.config.background.js",
    "clean": "rm -rf dist && mkdir -p dist",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vueuse/core": "^13.9.0",
    "vue": "^3.4.21"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "vite": "^5.2.0",
    "vite-plugin-web-extension": "^4.1.4"
  }
}
